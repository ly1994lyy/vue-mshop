import Vue from "vue";
import {
  Tabbar,
  TabbarItem,
  Icon,
  NavBar,
  Swipe,
  SwipeItem,
  Lazyload,
  Search,
  Form,
  Field,
  Button,
  Sidebar, 
  SidebarItem,
  Grid, 
  GridItem,
} from "vant";

Vue.use(Tabbar)
  .use(TabbarItem)
  .use(Icon)
  .use(NavBar)
  .use(Swipe)
  .use(SwipeItem)
  .use(Lazyload)
  .use(Search)
  .use(Form)
  .use(Field)
  .use(Button)
  .use(Sidebar)
  .use(SidebarItem)
  .use(Grid)
  .use(GridItem);
