import Vue from "vue";
import { Tabbar, TabbarItem, Icon,NavBar,Swipe, SwipeItem,Lazyload } from "vant";

Vue.use(Tabbar)
  .use(TabbarItem)
  .use(Icon)
  .use(NavBar)
  .use(Swipe)
  .use(SwipeItem)
  .use(Lazyload);
